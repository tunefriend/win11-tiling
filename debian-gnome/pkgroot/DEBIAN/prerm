#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        if [ -x /usr/bin/win11-tiling-setup ]; then
            /usr/bin/win11-tiling-setup --uninstall 2>/dev/null || true
        fi
        ;;
esac

exit 0